print(True and True)
a = [1,2,3]
del a[0]
print a

from sys import exit
print(not True)
#while a != '0': 循环语句
#with class as b:
#elif 条件语句
# global 全局 变量定义 global x 
# or逻辑运算
#assert  断言语句 如果发生错误按约定方式提醒
# if elif else
#pass 让程序什么也不做 
#yield 是一个生成器 可以用next()  调用其值
#break 跳出当前条件缩进部分 而非全局exit
#coutinue如果条件为真则继续执行 条件下的动作，
#try except finally  异常处理语句
#return 返回值  ，def定义函数  for 循环语句  lambda是一个表达式 可以快速嵌入函数